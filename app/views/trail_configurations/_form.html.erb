<%= form_with(model: trail_configuration, local: true) do |form| %>
  <% if trail_configuration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trail_configuration.errors.count, "error") %> prohibited this trail_configuration from being saved:</h2>

      <ul>
        <% trail_configuration.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :drug_id %>
    <%= form.collection_select :drug_id, Drug.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :clinic_id %>
    <%= form.collection_select :clinic_id, Clinic.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :doctor_id %>
     <%= form.collection_select :doctor_id, Doctor.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
